package _03_To_Do_List;

public class ToDoList {
	/*
	 * Create a program with five buttons, add task, view tasks, remove task, save list, and load list. 
	 * 
	 * The progam should also have an input field and a submit button
	 *
	 * When add task is clicked:
	 * 		ask the user for a task and save it to an array list
	 * 
	 * When the view tasks button is clicked:
	 *		show all the tasks in the list
	 * 
	 * When the remove task button is clicked:
	 * 		prompt the user for which task to remove and take it off of the list.
	 * 
	 * When the save list button is clicked:
	 * 		Save the list to a file
	 * 
	 * When the load list button is clicked:
	 * 		Prompt the user for the location of the file and load the list from that file
	 * 
	 * When the submit button is clicked:
	 *		If the user has not clicked a button ask the user to do so
	 *		If the user has clicked a button retrive the value from the input field and finish that buttons function
	 *
	 * When the program starts, it should automatically load the last saved file into the list. 
	 */
}
